import { HeaderLinks } from "../ResumeData";

const Header = () => {
  return (
    <header>
      <nav>
        <a href=""></a>
      </nav>
      <div>
        <ul></ul>
      </div>
    </header>
  );
};

export default Header;
